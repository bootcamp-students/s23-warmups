class Kata {
    static getVolumeOfCuboid(length, width, height) {
      // your code here
      var vol=width*length*height;
    return vol;
    }
  }

  // function is getVolumeOfCuboid, gives parameters, length, width height
  //variable created to calculate volume, l*w*h which is 'vol'
  //return vol 
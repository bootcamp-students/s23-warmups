function noSpace(x){
  return x.split(" ").join('') //If (" ") is used as separator, the string is split between these lines of letters in an array
  // join('') concatenates them using an empty string as a separator so theyre all joined without any space
}

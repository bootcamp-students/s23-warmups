function findUniq(arr) {
return arr.find(
(item) => arr.indexOf(item) === arr.lastIndexOf(item)
);
}


//BEGIN
//INIT
//loop through an array
//find an array method to identify a unique number in a set.
//END

//.find

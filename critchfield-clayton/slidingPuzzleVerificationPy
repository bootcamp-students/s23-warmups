# START
# Given a border of NxN
# Tiles labeled 0 to N^2 - 1 (example: a 3x3 would return [0,1,2][3,4,5][6,7,8])
# If the numbers are in order return true that the board is solved. 
# Otherwise return false
# END

def is_solved(board):
    for x in board:
        for y in x:
            if y != 0:
                return False
            else:
                return True

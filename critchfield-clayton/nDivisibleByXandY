//variables n, x, y
/* FUNCTION
      CHECK if n is divisble by x & y.
          RETURN true if so */

function isDivisible(n, x, y) {
  if (n % x === 0 && n % y === 0) {
    return true;
  } else {
    return false;
  }
}

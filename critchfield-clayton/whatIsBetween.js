/*Take two parameters
    START AN EMPTY ARRAY
      RETURN everything in an array between [a, b] using loop;
        THEN INCREMENT value a
          RETURN array
            END */

function between(a, b) {
  let array = [];
  for (a === ''; a <= b; a++) {
    array.push(a);
  }
  return array;
};

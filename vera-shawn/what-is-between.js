function between(a, b) {
    // find integers between two variables
    // create variable to hold value of a and b
    let array = [];
    
   for (i = a; i <= b; i++) {
      array.push(i);
    }
   
    // return them in an array including the variables
      return array;
  }
function isDivisible(n, x, y) {
    // is n divisible by x and y
    if (n % x === 0 && n % y === 0) {
      return true
      else 
        return false
    }
    // input must be positive
    
    // input must be non-zero numbers
    
  }
// BEGIN
// INIT 
  // function should return an element from the multidimensional array by the specified indices
  // lenth > 0
   
  function getElement(array, indexes) {
    // good luck
    let myIndex = array;
  for (let i = 0; i < indexes.length; i++) {
    const index = indexes[i];
    myIndex = index[index]
  }
  return index;
  }
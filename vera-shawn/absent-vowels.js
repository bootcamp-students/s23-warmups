//BEGIN
//INIT - define variables - define what is x?
function absentVowel(x){
    let vowels = [a, e, i, o, u];
      
      // declare a vowel index.
      
      let vowelIndex = null;

      // for of loop to find vowels of x
      for (vowel of x) {
      if (i = 0; i < x.length; i++) {
        
      }
    }
      // return index corresponding to the vowel array.
      //END
    }
function century(year) {
    // return the century that any given year falls within
    // math.ceil to get the upper number and divide by 100 to get the century.
    
    return Math.ceil(year/100);
  }
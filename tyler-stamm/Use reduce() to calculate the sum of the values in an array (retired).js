function sum(array) {
// INIT
  // return the sum of the values in the array
  // use .reduce to add the array items together and return a new array
  // declare new variable (integer) to host the sum of the original array
  // create new variable that reduces using the first variable equal to 0
  const newArr= 0;
  const sumWithInitial = array.reduce(
  (accumulator, currentValue) => accumulator + currentValue,
  newArr);
    return sumWithInitial;
}

def array_plus_array(arr1,arr2):
    # INIT
    #new variable to store sum of all elements
    # output number
    # arr1 and arr2 are lists
    # How do we combine all the elements of a list AND ADD THEM TOGETHER?
    # use sum to combine them all together
    return sum(arr1 + arr2)
    
    # END

class Kata {
  static getVolumeOfCuboid(length, width, height) {
    var vol=width*length*height;
  return vol;
  }
}

/* PSUEDOCODE

In order to get the volume of a cube:
  
  Length x Width x Height
  
Function to multiply the properties should get solution */

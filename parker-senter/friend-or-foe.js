function friend(friends){
    return friends.filter(friends => friends.length === 4);
}
  
  /* 
  PSEUDOCODE
  //take string of names
  //figure out which names have 4 letters
  //return names with 4 letters
  
  friends.filter(friends => friends.length === 4);
  */
class Kata {
    static getVolumeOfCuboid(length, width, height) {
      let sum = ((length*width)*height);
      return sum
    }
  }
  
  /*
  PUESDOCODE
  VARS:
  height
  width
  length
  
  FUNC:
  L*H*W
  */
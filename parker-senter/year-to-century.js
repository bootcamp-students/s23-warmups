function century(year) {
    let yearToCent = Math.ceil(year/100)
    return yearToCent;
  }
  
  /*
  PUESDOCODE
  VAR:
  century - what century is it
  year - what year is it
  yearToCent - what century the year is in
  
  FUNC:
  function century(year) {
   yearToCent = Math.ceil(year/100)
   return yearToCent;
  }
  */
  
  /*ANOTHER WAY SHAWN FIGURED OUT

  function century(year) {
    return Math.ceil(year/100);
  }
  */
function between(a, b) {
  var numbers = []
  for (var i = a; i <= b; i++) {
    numbers.push(i);
  }
  return numbers;
}
//create variable to hold integerds
//create a loop that evaluates all numbers within array
//allow evaluation to take over new array
//return numbers

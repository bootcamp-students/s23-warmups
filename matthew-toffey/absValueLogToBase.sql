/*  SQL  */
SELECT ABS(number1) as abs, LOG(64, number2) as log 
FROM decimals

--BEGIN
--Given a table of decimals
--must change the presentation of sumbers using LOG and ABS values in new columns abs and log
--end

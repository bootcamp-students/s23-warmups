function between(a, b) {
  // define the array to place all integers
  //for loop of a < b a++, insert  an i variable to insert the integers
   let arr =[];
  for (i=a;i<=b;i++) {
          arr.push(i);
  //incrementing for loop to insert integers
  // your code here
      }
        return arr;
}

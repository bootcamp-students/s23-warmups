// Math.ceil rounds up to the nearest integer
// When dividing the year by 100 it will turn it into a decimal 
// Math.ceil will round the decimal up to the answer

function century(year) {
  // Finish this :)
  return Math.ceil(year / 100)
}

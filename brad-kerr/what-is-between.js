// create a variable that is an empty array
// create a loop to count from a to b
// push the loop to the array
// return the array

function between(a, b) {
  let array = [];
  
  for (a === " "; a<=b; a++){
    array.push(a);
  }
  return array;
}
